jobs:
 - template: default.yml@templates
   parameters:
     minrust: 1.44.1
     clippy: false

resources:
  repositories:
    - repository: templates
      type: github
      name: iceiix/azure-pipelines
      ref: refs/heads/clippy
      endpoint: iceiix

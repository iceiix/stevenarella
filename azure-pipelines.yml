jobs:
 - template: default.yml@templates
   parameters:
     minrust: 1.44.1
     clippy: -A clippy::all -A clippy::temporary_cstring_as_ptr -A clippy::not_unsafe_ptr_arg_deref -A clippy::float_cmp

resources:
  repositories:
    - repository: templates
      type: github
      name: iceiix/azure-pipelines
      ref: 11424963ee96daf8cb5cd6b546cb79f168be298b
      endpoint: iceiix

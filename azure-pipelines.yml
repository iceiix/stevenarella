jobs:
 - template: default.yml@templates
   parameters:
     minrust: 1.44.1

resources:
  repositories:
    - repository: templates
      type: github
      name: crate-ci/azure-pipelines
      ref: refs/heads/v0.4
      endpoint: iceiix

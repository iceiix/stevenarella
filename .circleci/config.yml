version: 2.1
executors:
    docker:
        parameters:
            version:
                type: string
                default: 1.35.0
        docker:
            - image: circleci/rust:<< parameters.version >>
jobs:
    build:
        executor: docker
        steps:
            - run:
                name: The First Step
                command: |
                    echo 'Hello world!'
                    echo 'This is the delivery pipeline'

